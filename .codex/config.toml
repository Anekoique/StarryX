# Project-scoped Codex defaults for Ark workflows.
# Codex loads this after ~/.codex/config.toml when working in this project.

# Keep AGENTS.md as the primary project instruction file.
project_doc_fallback_filenames = ["AGENTS.md"]
